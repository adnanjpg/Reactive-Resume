setup:
  config:
    env_file: .env
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
    - plan: papertrail:choklad
      as: LOGS

build:
  docker:
    web:
      dockerfile: ./Dockerfile

run:
  web: dumb-init pnpm run start
  chrome:
    image: Dockerfile.chrome
  minio:
    image: Dockerfile.minio
    command:
      - server /data
